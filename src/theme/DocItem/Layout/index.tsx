/**
 * Pass-through — Giscus is rendered from Layout (see src/theme/Layout) so every
 * /docs/* route gets it, including category index pages that do not use this layout.
 */
export {default} from '@theme-original/DocItem/Layout';
