---
title: Templates Import / Export
sidebar_position: 1
slug: /templates-import-export
description: Bulk-import WhatsApp templates from JSON, or export a project's templates to JSON, via the Texter v2 API.
wrapperClassName: tplImportWide
---

import TemplatesImportTool from '@site/src/components/TemplatesImport/TemplatesImportTool';

:::caution[Privacy / credentials]
Your API key is sent from this browser to the Texter Docs proxy and forwarded to `*.texterchat.com`. It is never stored, but anyone with access to this tab can intercept it — use a dedicated key with the minimum scopes you need and revoke it when finished.
:::

<TemplatesImportTool />
